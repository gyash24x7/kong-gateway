apiVersion: configuration.konghq.com/v1
kind: KongClusterPlugin
metadata:
  name: actuator-plugin
  annotations:
    kubernetes.io/ingress.class: kong
  labels:
    global: "true"
config:
  AUTH_HEADER_NAME: "Authorization"
plugin: actuator-plugin

